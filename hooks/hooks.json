{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "if [ -f .pre-commit-config.yaml ] && [ -n \"$CLAUDE_FILE_PATHS\" ]; then out=$(pre-commit run --files $CLAUDE_FILE_PATHS 2>&1) || { printf '%s\\n' \"$out\" >&2; exit 2; }; fi",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
